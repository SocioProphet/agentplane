# vm.nix (placeholder)
# Next turn we will make this a minimal NixOS VM module that:
# - mounts /mnt/artifacts
# - runs a tiny "agent" service
# - emits proof artifacts into /mnt/artifacts
{ config, pkgs, lib, ... }:
{
  # intentionally empty for now
}
